# humanoid robot

Placeholder for future humanoid model.

MuJoCo ships a humanoid model you can use as a starting point:
    import mujoco
    # Find the built-in humanoid:
    import os
    mujoco_path = os.path.dirname(mujoco.__file__)
    print(mujoco_path)  # look in model/ subfolder
